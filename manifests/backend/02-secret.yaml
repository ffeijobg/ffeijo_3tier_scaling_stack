# manifests/backend/02-secret.yaml
# Intentionally empty: the backend has no credentials of its own today.
# It authenticates to Postgres using postgres-secret, owned by the database
# tier (see database/01-secret.yaml) and referenced via secretKeyRef in
# backend/03-deployment.yaml. Add a Secret here only if the backend gains an
# independent credential (e.g. a signing key or third-party API token).
