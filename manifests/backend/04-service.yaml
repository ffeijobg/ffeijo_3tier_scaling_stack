# manifests/backend/04-service.yaml
 
apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: apps
  labels:
    app: backend
    tier: backend
spec:
  type: ClusterIP
  selector:
    app: backend
  ports:
    - port: 8000
      targetPort: 8000
      name: http
